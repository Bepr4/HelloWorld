[project]
name = "module1-v1"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "crawl4ai>=0.8.0",
  "fastapi",
  "pydantic",
  "uvicorn",
]

[tool.pytest.ini_options]
addopts = "-p no:cacheprovider"
pythonpath = ["src"]
testpaths = ["tests"]
